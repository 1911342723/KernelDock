# 网关（FastAPI 服务）运行时依赖 —— 与沙箱镜像的 requirements.txt 分离。
# 沙箱全家桶（bokeh/plotly/scipy/sklearn/statsmodels/wordcloud 等）只属于
# 沙箱镜像；网关仅保留 API 框架 + Docker 管理 + 文件 schema 提取所需。

fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.3
python-multipart>=0.0.9

docker>=7.0
requests>=2.32
httpx>=0.27        # 分布式 router 心跳（services/router_heartbeat.py）
psutil>=5.9
sentry-sdk>=2.0

# 文件上传 schema 提取（file_manager 读 csv/excel/parquet 表头与采样）
pandas>=2.2
pyarrow>=16.0
openpyxl>=3.1
xlrd>=2.0
python-dateutil>=2.9

# 注意：SANDBOX_ALLOW_LOCAL_FALLBACK=true（本地 subprocess 执行用户代码，
# 仅限开发调试）时需要完整的 requirements.txt；生产部署保持 false 即可。
