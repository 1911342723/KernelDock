# Trivy 漏洞豁免登记（image-scan 用）
#
# 用法：每行一个 CVE ID，命中的漏洞会被扫描忽略。
# 仅在「已人工评估确认不可利用 / 上游无修复 / 已有补偿控制」时登记，
# 并写明理由与复核日期，避免变成无脑屏蔽。
#
# 示例：
# CVE-2024-XXXXX  # debian libfoo，沙箱无网络+非 root，不可利用；2026-06-13 评估
#
# 当前无豁免项（基线 pip 依赖审计 0 漏洞）。
